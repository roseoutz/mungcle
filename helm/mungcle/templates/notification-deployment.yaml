apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification
  namespace: {{ include "mungcle.namespace" . }}
  labels:
    {{- include "mungcle.labels" . | nindent 4 }}
    {{- include "mungcle.selectorLabels" (dict "component" "notification") | nindent 4 }}
spec:
  replicas: {{ .Values.services.notification.replicas }}
  selector:
    matchLabels:
      {{- include "mungcle.selectorLabels" (dict "component" "notification") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mungcle.selectorLabels" (dict "component" "notification") | nindent 8 }}
    spec:
      containers:
        - name: notification
          image: {{ include "mungcle.image" (dict "registry" .Values.global.image.registry "image" .Values.services.notification.image "tag" .Values.services.notification.tag) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.services.notification.port }}
          envFrom:
            - configMapRef:
                name: mungcle-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mungcle-secret
                  key: postgres-password
            - name: TSID_NODE
              value: {{ .Values.services.notification.tsidNode | quote }}
          readinessProbe:
            grpc:
              port: {{ .Values.services.notification.port }}
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            grpc:
              port: {{ .Values.services.notification.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          resources:
            {{- toYaml .Values.services.notification.resources | nindent 12 }}
