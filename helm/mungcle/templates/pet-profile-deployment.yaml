apiVersion: apps/v1
kind: Deployment
metadata:
  name: pet-profile
  namespace: {{ include "mungcle.namespace" . }}
  labels:
    {{- include "mungcle.labels" . | nindent 4 }}
    {{- include "mungcle.selectorLabels" (dict "component" "pet-profile") | nindent 4 }}
spec:
  replicas: {{ .Values.services.petProfile.replicas }}
  selector:
    matchLabels:
      {{- include "mungcle.selectorLabels" (dict "component" "pet-profile") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mungcle.selectorLabels" (dict "component" "pet-profile") | nindent 8 }}
    spec:
      containers:
        - name: pet-profile
          image: {{ include "mungcle.image" (dict "registry" .Values.global.image.registry "image" .Values.services.petProfile.image "tag" .Values.services.petProfile.tag) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.services.petProfile.port }}
          envFrom:
            - configMapRef:
                name: mungcle-config
            - configMapRef:
                name: mungcle-config-db
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mungcle-secret
                  key: postgres-password
            - name: TSID_NODE
              value: {{ .Values.services.petProfile.tsidNode | quote }}
          readinessProbe:
            grpc:
              port: {{ .Values.services.petProfile.port }}
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            grpc:
              port: {{ .Values.services.petProfile.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          resources:
            {{- toYaml .Values.services.petProfile.resources | nindent 12 }}
